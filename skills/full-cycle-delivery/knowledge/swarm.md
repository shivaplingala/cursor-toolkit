# Swarm (FCD-V2) — delivery craft

- Escalate ≥ 2 required for Ruflo swarm; single “user asked swarm” alone is not enough.
- File board + `progress.md` beat Ruflo memory on conflict; claims mirror locks.
- Workers must still run verify + coding/impact to 0 — protocol does not replace FCD gates.
- Map tasks: backend / frontend / qa-engineer (same agents as classic).
- Never `ruflo init` inside an open workspace unless the user explicitly wants project-local install.
