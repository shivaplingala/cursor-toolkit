# Build — delivery craft

- Prefer **ponytail** for all code; agency personas are Phase 2 design only.
- Progress ledger (`progress.md`) is the resume source — create after plan approval; never re-grill locked Grill-Q&A.
- Host installs: shared `host-discovery.sh`; Antigravity = **copy** skills (not symlink) + dual rules (`.mdc` + stripped `.md`).
- FCD Phase 4 agents: `backend-implementer` / `frontend-implementer` / `qa-engineer` — QA catalog before FE harness when both planned.
- Global FCD plugin does **not** contain `agency-engineering`; resolve from `serverless-monorepo/.cursor/skills/agency-engineering/`.
