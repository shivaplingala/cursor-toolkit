---
name: agents-build
description: >-
  Add AgentCore capabilities (memory, VPC, multi-agent, browser, code
  interpreter, app integrate, migrate, teardown). Bundled with
  aws-diagnose; use after /aws-diagnose or /agents-debug when the
  fix is a capability change.
---

# /agents-build

Follow **agents-build** in this plugin (`skills/agents-build/SKILL.md`).

If you arrived from `/aws-diagnose`, the diagnose report is read-only —
confirm with the user before running `agentcore add|deploy` or other writes.
